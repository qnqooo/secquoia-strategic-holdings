name: build

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Validate Aggy production loader
        shell: bash
        run: |
          set -euo pipefail
          grep -Fq 'aggy-embed.js?v=1.3.0-rc.2-languages11-20260805' index.html
          grep -Fq 'aggy-embed.js?v=1.3.0-rc.2-languages11-20260805' 404.html
          grep -Fq 'data-aggy-crypto-profile="E2EE/PQC"' index.html
          grep -Fq 'data-aggy-crypto-profile="E2EE/PQC"' 404.html
